{
  "name": "mangamotive",
  "version": "1.0.0",
  "description": "Dependencies :     - express     - mongoose     - node           - concurrently(in global file )         - nodemon (To start the index.js in the backend everytime when it gets edited )     - react(frontend )",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend": " cd frontend && npm start",
    "backend": " cd backend && npm start",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.0",
    "react-router-dom": "^6.28.0"
  }
}
